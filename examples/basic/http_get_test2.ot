use http

fn main():
    println("Testing http.get with simple URL...")
    let url = "http://example.com"
    let body = http.get(url)
    
    if body == "":
        println("Body is empty!")
    else:
        println(f"Body length: {len(body)}")
        println("First 100 chars:")
        println(body)
